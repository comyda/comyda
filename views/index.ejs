<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <title>oxiFood</title>
    <link rel="stylesheet" href="index.css">
    <link rel="shortcut icon" href="favicon.ico" type="image/x-icon">
  </head>
  <body>
    <header>
      <h1>oxiFood</h1>
    </header>

  <div class="wrapper">
  <div class="content">
    <div class="button-container" id= "criar">
      <a class="button" href="/evento" >Criar evento</a>
    </div>

    <% for(var i=0; i<eventos.length; i++) {%>

    <li class="order">
      <div class="img-restaurant" style="background-image: url(img/<%= eventos[i].imgRestaurant %>)"></div>

      <div class="who">
        <a class="nomedoevento" href="/eventos/<%= eventos[i]._id%>/<%= eventos[i].status%>"><%= eventos[i].name%></a> por
        <span class="ownername"><%= eventos[i].ownername %></span> está pedindo em <span class="restaurant"><%= eventos[i].restaurant%></span>
      </div>

      <br>

      <div class="closes">
        O pedido encerra em <%= eventos[i].finishedTime %> <a id="encerrado"><%= eventos[i].status %></a>
      </div>

      <div class="delivery">
        O evento acontecerá em  <%= eventos[i].timeAsString%>
      </div>
    </li>
    <% } %>

    <% if(eventos.length === 0) {%>
      <p id="criar"> Não há eventos no momento, crie o primeiro! =)<p>
    <% } %>

</div>
  <!--RODAPÉ-->
  <footer>
    <div id="rodape">
      <p>Made in Recife, oxi!</p>
      <p>Por Leonardo Oliveira e Rayana Gonçalves at
      <a href="https://www.thoughtworks.com/" target="_blank" style="color: #696969">
      Thoughtworks</a></p>
    </div>
  </footer>
 <!--FIM DO RODAPÉ-->

    </div>
  </body>
</html>
