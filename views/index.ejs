<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <title>oxiFood</title>
    <link rel="stylesheet" href="index.css">
    <link rel="shortcut icon" href="favicon.ico" type="image/x-icon">
  </head>
  <body>
    <header>
      <h1>oxiFood</h1>
    </header>

  <div class="wrapper">
  <div class="content">
    <div class="button-container" id= "criar">
      <a class="button" href="/evento" >Criar evento</a>
    </div>

    <% for(var i=0; i<eventos.length; i++) {%>

    <li class="order">


      <div>
        <a id="nomedoevento" href="/eventos/<%= eventos[i]._id%>/<%= eventos[i].status%>"><%= eventos[i].name%></a>
      </div>

      <div class="who">
        <%= eventos[i].ownername %> está pedindo em <%= eventos[i].restaurant%>
      </div>


      <div class="closes">
        O pedido encerra em <%= eventos[i].finishedTime %> <a id="encerrado"><%= eventos[i].status %></a>
      </div>

      <div class="delivery">
        <class="is-desktop">O evento acontecerá em  <%= eventos[i].timeAsString%>
      </div>

      <div class="clear"></div>

    </li>
    <% } %>

    <% if(eventos.length === 0) {%>
      <p id="criar"> Não há eventos no momento, crie o primeiro! =)<p>
    <% } %>

</div>
  <!--RODAPÉ-->
  <footer>
    <div id="rodape">
      <p>Made in Recife, oxi!</p>
      <p>Por Leonardo Oliveira e Rayana Gonçalves at
      <a href="https://www.thoughtworks.com/" target="_blank" style="color: #696969">
      Thoughtworks</a></p>
    </div>
  </footer>
 <!--FIM DO RODAPÉ-->

    </div>
  </body>
</html>
